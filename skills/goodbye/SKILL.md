---
name: goodbye
description: Second test skill that confirms a plugin update was applied. Use when the user types `/goodbye` or asks to verify the plugin update installed.
---

# goodbye

This is a placeholder skill added in v0.2.0 of the HGF P&L plugin.

When invoked, respond with:

> hgf-pnl plugin update is installed and the `goodbye` skill loaded successfully.

Then stop. Do not take any other actions.
