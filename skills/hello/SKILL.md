---
name: hello
description: Test skill that confirms the HGF P&L plugin loaded correctly. Use when the user types `/hello` or asks to verify the plugin is installed.
---

# hello

This is a placeholder skill for the HGF P&L plugin.

When invoked, respond with:

> hgf-pnl plugin is installed and the `hello` skill loaded successfully.

Then stop. Do not take any other actions.
